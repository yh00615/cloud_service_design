name: Quality Check

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run comprehensive review
        id: review
        run: |
          npm run review
          echo "review_completed=true" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Upload review report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: review-report
          path: REVIEW_REPORT.json
          retention-days: 30

      - name: Comment PR with results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            try {
              const report = JSON.parse(fs.readFileSync('REVIEW_REPORT.json', 'utf8'));
              
              // 등급별 이모지
              const gradeEmoji = {
                'A': '🌟',
                'B': '✅',
                'C': '⚠️',
                'D': '❌',
                'F': '🚨'
              };
              
              // 점수별 색상
              const getScoreColor = (score) => {
                if (score >= 90) return '🟢';
                if (score >= 80) return '🟡';
                if (score >= 70) return '🟠';
                return '🔴';
              };
              
              // 주요 문제 요약
              const codeIssues = report.code.issues
                .filter(i => i.severity === 'error')
                .slice(0, 3)
                .map(i => `- ❌ **${i.category}**: ${i.message} (${i.count}개)`)
                .join('\n');
              
              const guideIssues = report.guides.issues
                .filter(i => i.severity === 'error')
                .slice(0, 3)
                .map(i => `- ❌ **${i.category}**: ${i.message} (${i.count}개)`)
                .join('\n');
              
              // 권장사항
              const recommendations = report.overall.recommendations
                .slice(0, 3)
                .map((rec, idx) => {
                  const priorityEmoji = rec.priority === 'high' ? '🔴' : rec.priority === 'medium' ? '🟡' : '🟢';
                  return `${idx + 1}. ${priorityEmoji} **[${rec.priority.toUpperCase()}]** ${rec.message}\n   → ${rec.action}`;
                })
                .join('\n\n');
              
              const comment = `
            ## ${gradeEmoji[report.overall.grade]} 종합 검토 결과

            ### 📊 전체 점수
            ${getScoreColor(report.overall.score)} **${report.overall.score}점** (${report.overall.grade}등급)

            ---

            ### 📦 소스코드 검토
            | 항목 | 결과 |
            |------|------|
            | 총 검사 | ${report.code.total}개 |
            | ✅ 통과 | ${report.code.passed}개 |
            | ❌ 실패 | ${report.code.failed}개 |
            | ⚠️ 경고 | ${report.code.warnings}개 |

            ${codeIssues ? '**주요 문제:**\n' + codeIssues : '✅ 문제 없음'}

            ---

            ### 📚 실습 가이드 검토
            | 항목 | 결과 |
            |------|------|
            | 총 검사 | ${report.guides.total}개 |
            | ✅ 통과 | ${report.guides.passed}개 |
            | ❌ 실패 | ${report.guides.failed}개 |
            | ⚠️ 경고 | ${report.guides.warnings}개 |

            ${guideIssues ? '**주요 문제:**\n' + guideIssues : '✅ 문제 없음'}

            ---

            ${recommendations ? '### 💡 권장사항\n\n' + recommendations + '\n\n---\n' : ''}

            ### 📋 다음 단계
            ${report.overall.score >= 80 
              ? '✅ **배포 준비 완료!** 모든 기준을 충족했습니다.' 
              : `⚠️ **개선 필요**: 오류를 수정한 후 다시 커밋하세요.

            \`\`\`bash
            # 로컬에서 검증
            npm run review

            # 오류 자동 수정 시도
            npm run lint:fix
            npm run fix:auto

            # 재검증
            npm run review
            \`\`\`
            `}

            ---

            <details>
            <summary>📄 상세 리포트 보기</summary>

            전체 리포트는 [Artifacts](${context.payload.pull_request.html_url}/checks)에서 다운로드할 수 있습니다.

            </details>
              `;
              
              // 기존 코멘트 찾기
              const { data: comments } = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
              });
              
              const botComment = comments.find(comment => 
                comment.user.type === 'Bot' && 
                comment.body.includes('종합 검토 결과')
              );
              
              // 기존 코멘트 업데이트 또는 새로 생성
              if (botComment) {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: botComment.id,
                  body: comment
                });
              } else {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  body: comment
                });
              }
              
            } catch (error) {
              console.error('리포트 파일을 읽을 수 없습니다:', error);
              
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: '⚠️ 종합 검토 중 오류가 발생했습니다. 로그를 확인해주세요.'
              });
            }

      - name: Check quality threshold
        if: always()
        run: |
          if [ -f REVIEW_REPORT.json ]; then
            SCORE=$(node -p "require('./REVIEW_REPORT.json').overall.score")
            ERRORS=$(node -p "require('./REVIEW_REPORT.json').code.failed + require('./REVIEW_REPORT.json').guides.failed")
            
            echo "📊 품질 점수: ${SCORE}점"
            echo "❌ 오류 개수: ${ERRORS}개"
            
            if [ $SCORE -lt 80 ]; then
              echo "::error::품질 점수가 80점 미만입니다 (현재: ${SCORE}점)"
              exit 1
            fi
            
            if [ $ERRORS -gt 0 ]; then
              echo "::error::오류가 ${ERRORS}개 있습니다. 모든 오류를 수정해주세요."
              exit 1
            fi
            
            echo "✅ 품질 기준을 충족했습니다!"
          else
            echo "::error::REVIEW_REPORT.json 파일을 찾을 수 없습니다"
            exit 1
          fi
